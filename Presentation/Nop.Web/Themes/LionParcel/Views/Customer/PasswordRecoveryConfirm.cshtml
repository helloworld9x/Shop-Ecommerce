@model PasswordRecoveryConfirmModel
@{
    Layout = "~/Views/Shared/_ColumnsOne.cshtml";

    //title
    Html.AddTitleParts(T("PageTitle.PasswordRecovery").Text);
}
@using Nop.Web.Models.Customer;
<div class="page password-recovery-page">
    <div class="page-title">
        <h1>@T("Account.PasswordRecovery")</h1>
    </div>
    <div class="page-body">
        @if (!String.IsNullOrEmpty(Model.Result))
        {
            <div class="result">
                @Model.Result
            </div>
        }
        @if (!Model.DisablePasswordChanging)
        {
            using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                <div class="fieldset">
                    <div class="form-fields">
                        <div class="inputs">
                            @Html.LabelFor(model => model.NewPassword, new { }, ":")
                            <div class="control">
                                @Html.EditorFor(model => model.NewPassword, new { @class = "single-line" )
                                @Html.RequiredHint()
                                @Html.ValidationMessageFor(model => model.NewPassword)
                            </div>
                        </div>
                        <div class="inputs">
                            @Html.LabelFor(model => model.ConfirmNewPassword, new { }, ":")
                            <div class="control">
                                @Html.EditorFor(model => model.ConfirmNewPassword, new { @class = "single-line" )
                                @Html.RequiredHint()
                                @Html.ValidationMessageFor(model => model.ConfirmNewPassword)
                            </div>
                        </div>
                    </div>
                </div>
                <div class="buttons text-align-center">
                    <input type="submit" name="set-password" class="btn btn-primary" value="@T("Account.PasswordRecovery.ChangePasswordButton")" />
                </div>
            }
        }
    </div>
</div>
